
import {
    ADD_TO_CART,
    REMOVE_TO_CART,
    SAVE_SHIPPING_INFO 
} from "../constant/CartConstant"

export const cartReducer = (state = {cartItems: []}, action) => {

    switch(action.type) {
        case ADD_TO_CART:
            return {
                
            }
    }
}